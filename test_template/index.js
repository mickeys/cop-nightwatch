// This is a template for writing your own tests in Nightwatch

/* module.exports = {
    // This is the very basic foundation of a test
    // First, put the test name and then create the function body. Note: You have to pass in the browser object
    'Test Name': function(browser) {
        // First you would need to navigate to a URL
        browser
            .url('http://www.whateveryouwant.com');
        // Second you would need to do all of your assertions for the test
        browser
            .assert.visible('something')
            .expect('something').to.be.visible;
        // Once you have all your test steps complete, you NEED to end the browser.
        // If you do not do this step, your browser stays open and does not close.
        browser
            .end();
    }
} */
